# import * from './generated/prisma.graphql'

type Query {
  items: [Item]!
}

type Mutation {
  createItem(
    price: Int
    title: String
    image: String
    largeImage: String
    description: String
  ): Item!
}
