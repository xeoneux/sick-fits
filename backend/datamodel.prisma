type User {
  id: ID! @unique
  name: String!
  email: String!
}

type Item {
  id: ID! @unique
  price: Int!
  title: String!
  image: String
  largeImage: String
  description: String!
  # createdAt: DateTime!
  # updatedAt: DateTime!
  # user: User!
}
